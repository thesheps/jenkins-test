node {
    stage("Deploy to Dev") {
        sh "echo 'Deploying to Dev...'"
    }

    stage("Promote to Int") {
        input("Proceed to Int?")
    }

    stage("Deploy") {
        build(job: 'int-deploy/master', wait: false)
    }
}