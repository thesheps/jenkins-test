pipeline {
    agent any

    stages {
        stage("Build") {
            steps {
                sh "echo 'Building...'"
            }
        }

        stage("Deploy") {
            steps {
                build(job: 'dev-deploy/master', propagate: false)
            }
        }
    }
}